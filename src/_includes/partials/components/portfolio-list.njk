{% if portfolioListItems.length %}
  <section class="post-list">
    <div class="post-list__wrapper">
      <h2 class="post-list__heading">{{ portfolioListHeading }}</h2>
      <ol class="post-list__items" reversed>
        {% for item in portfolioListItems %}
          {% if item.date.getTime() <= global.now %}
            <li class="post-list__item">
              <h3>
                <a href="{{ item.url }}" class="post-list__link" rel="bookmark">{{ item.data.title }}</a>
              </h3>
            </li>
          {% endif %}
        {% endfor %}
      </ol>
    </div>
  </section>
{% endif %}
