{% extends 'layouts/base.njk' %}
{% from "macros/macros.njk" import component %}

{# Intro content #}
{% set introHeading = title %}

{% block content %}
  <main id="main-content" tabindex="-1">
    <h3 class="portfolio__client">{{client}}</h3>
    <h1 class="portfolio__title">{{title}}</h1>

    <div class="portfolio__body">
      {{ content | safe }}
      {% include 'partials/components/page-sections-portfolio.njk' %}
    </div>
  </main>
{% endblock %}

{{ content | safe }}
