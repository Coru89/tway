---
label: Service Panel
hide_body: true
fields:
- name: title
  type: text
  config:
    required: false
  label: Title
- name: image
  type: file
  config:
    maxSize: 64
  label: image
- name: links
  type: field_group_list
  fields:
  - name: linkText
    type: text
    config:
      required: false
    label: Link Text
  - name: linkPath
    type: text
    config:
      required: false
    label: path
  config:
    min: 
    max: 
    labelField: 
  label: Links
